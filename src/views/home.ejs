<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('./partials/_header') %>
    </head>

    <body>
        <% if (req.isAuthenticated()) { %>
        <h1>Welcome, <%= req.user.username %></h1>
        <% } else { %>
        <h1>Not logged in</h1>
        <% } %>
    </body>
</html>
